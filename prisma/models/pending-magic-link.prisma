model PendingMagicLink {
  id        String   @id @default(uuid())
  email     String
  tenantId  String
  createdAt DateTime @default(now())
  used      Boolean  @default(false)

  @@index([email])
}
